import React from 'react';

const AddToCart = ({pro}) => {
    return (
        <div>
            
            <h2 className='some-style text-center'>{pro.name}</h2>
            
        </div>
    );
};

export default AddToCart;