import React from 'react';

const AddToCart = ({pro}) => {
    return (
        <div>
            
            <h3 className='some-style text-center'>{pro.name}</h3>
            
        </div>
    );
};

export default AddToCart;